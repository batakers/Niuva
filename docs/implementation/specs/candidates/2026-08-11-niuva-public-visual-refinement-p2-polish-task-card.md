# Candidate Task Card — Niuva Public Visual Refinement P2 Polish

Status: `AUTHORIZED FOR PROTOTYPE-ONLY EDITS`

## Objective

Close the remaining non-blocking visual and review-fixture issues from the R12
formal critique without changing production source, canonical authority, route
decisions, provider behavior, or readiness status.

## Authority and boundaries

- Baseline: isolated prototype worktree from `origin/main` (`954837c`).
- Applicable evidence: `FORMAL_EXPERT_CRITIQUE_RERUN_3.md` (R12,
  `PASS WITH CONDITIONS`, P0/P1 clear).
- Impeccable is the primary visual workflow; the named frontend/design skills
  are references only.
- Prototype-only files under
  `docs/implementation/prototypes/2026-08-10-niuva-public-visual-refinement-prototype-r1/`.
- No production application code, API, database, provider, deployment,
  canonical document, commit, push, PR, merge, or go-live action.

## In scope

1. Make the sticky header opaque so state-focused content does not ghost
   through it.
2. Improve Projects first-view scan by keeping each artifact immediately tied
   to a factual project label/title while preserving the editorial composition.
3. Make the deferred Retail path visibly deferred without activating a route.
4. Make the map-unavailable copy explicitly honest and actionable through the
   existing Contact form, without inventing an address or provider link.
5. Label the persistence-failure Review fixture as a failure that appears after
   submit, not as an already-failed initial form.

## Acceptance criteria

- No new gradients, glassmorphism, generic card grid, emoji, external request,
  or invented business facts.
- Participant Mode contains no evaluator/review vocabulary.
- Existing B2B form/WhatsApp state machine and deep-link asset behavior remain
  intact.
- Detector, syntax checks, contract tests, and focused browser checks pass at
  390px and desktop widths; local server is stopped afterward.
- Final result remains prototype-only and `PASS WITH CONDITIONS`; a browser
  pass does not authorize moderated research or production implementation.

## Intentionally unchanged

Retail transaction, Admin/CMS, payment, upload, map provider, canonical
decisions, and all production source remain untouched.
