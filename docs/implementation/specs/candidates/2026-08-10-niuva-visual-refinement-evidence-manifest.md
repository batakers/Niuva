# Candidate Visual Refinement Evidence Manifest

**Status:** Candidate supporting artifact — Context Only
**Date:** 10 August 2026
**Baseline:** `origin/main` at `954837c9dd4fcaeb9438c16fb6934210e082a364`
**Parent packet:** [Candidate Visual Refinement Direction Packet](2026-08-10-niuva-visual-refinement-direction-packet.md)

This manifest records the evidence boundary for the read-only visual audit. It
does not make screenshots canonical, add assets to the product, or authorize
source changes.

## 1. Evidence location

The current audit captures are local-only at:

`C:\tmp\niuva-visual-audit-evidence-20260810-scrolled`

The folder contains scrolled desktop and mobile captures for Home, Projects,
and Contact. It is intentionally outside the repository because this packet is
not yet a publication or implementation artifact.

## 2. Capture matrix

| Route | Desktop | Mobile | Review purpose |
| --- | --- | --- | --- |
| `/` | 1440px | 390px | B2B-primary hierarchy, hero artifact, evidence rhythm, CTA placement |
| `/projects` | 1440px | 390px | Artifact-first reading, case-study composition, repeated-container risk |
| `/contact` | 1440px | 390px | Inquiry hierarchy, form clarity, response contract, map fallback |

The next wireframe/prototype pass must add 768px and 1024px captures and record
the exact viewport height, route state, and asset fixture used for each frame.

## 3. Checks performed

- Browser console: no errors or warnings in the captured routes.
- 390px horizontal overflow: none observed.
- Impeccable mechanical detector: `[]` for the audited public source scope.
- Source baseline: clean detached worktree from `origin/main`.
- Temporary audit server, junction, and capture script: removed after review.

## 4. Evidence limitations

- A clean detector result is not evidence of visual specificity, brand fit,
  truthful content, accessibility conformance, or implementation readiness.
- The external map may render as unavailable in a local or restricted
  environment; the next artifact must show the explicit fallback state.
- The current set does not prove tablet composition, keyboard completion,
  screen-reader announcements, dark-mode behavior, or moderated usability.
- Screenshots do not authorize copying the R6 bounded prototype into production.

## 5. Asset and provenance contract

The next task card must identify each visible project image or media item with:

1. source path or approved media reference;
2. whether it is authentic Niuva evidence or a clearly labelled synthetic
   fixture;
3. owner for caption, alt text, and publication permission; and
4. replacement action if the asset is unavailable or unapproved.

No stock image, generated project result, client logo, metric, testimonial, or
technical telemetry may be presented as Niuva evidence without an approved
provenance record.

## 6. Reproduction handoff

The next reviewer should regenerate the matrix from a fresh worktree at the
declared baseline, use the repository's documented frontend start command, and
record the resulting capture paths in the task/PR evidence. Do not rely on this
local-only folder as proof that a later source change remains visually aligned.

## 7. Publication boundary

This manifest and the parent packet remain candidate documentation. Publishing
them requires a separate exact-path stage/commit/push/PR approval. No canonical
register or decision record is updated by this manifest.
