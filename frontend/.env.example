# Public HTTPS origin, no trailing slash.
REACT_APP_PUBLIC_SITE_URL=https://www.example.com

# Backend HTTPS origin, no /api suffix and no trailing slash.
REACT_APP_BACKEND_URL=https://api.example.com

# Do not expose source maps in a public release unless uploaded privately to an error tracker.
GENERATE_SOURCEMAP=false
